{
  "name": "airport-task-planner-backend",
  "version": "1.0.0",
  "description": "Airport Task Planner - Backend API for managing operational airport tasks",
  "main": "server.mjs",
  "type": "module",
  "scripts": {
    "start": "node server.mjs",
    "dev": "nodemon server.mjs",
    "build": "echo 'Backend build completed - no compilation needed'",
    "migrate": "knex migrate:latest --knexfile knexfile.cjs",
    "migrate:rollback": "knex migrate:rollback --knexfile knexfile.cjs",
    "seed": "knex seed:run --knexfile knexfile.cjs",
    "migrate:reset": "knex migrate:rollback --all --knexfile knexfile.cjs && knex migrate:latest --knexfile knexfile.cjs",
    "setup-db": "npm run migrate && npm run seed"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.6.1",
    "express": "^4.21.2",
    "jsonwebtoken": "^9.0.2",
    "knex": "^3.1.0",
    "nodemon": "^3.1.10",
    "pg": "^8.16.3"
  }
}
